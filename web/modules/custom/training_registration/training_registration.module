<?php
function training_registration_mail($key, &$message, $params) {
  switch ($key) {
    case 'registration_mail':
      $message['subject'] = $params['subject'];
      $message['body'][] = $params['message'];
      break;
  }
}