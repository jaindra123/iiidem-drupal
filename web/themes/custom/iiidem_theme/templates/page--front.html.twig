
{% include '@iiidem_theme/components/header/header.twig' with {
  logo: logo,
  site_name: site_name,
  page: page
} %}

<main>
    {{ content.field_components }}

    <!-- Carousel Start -->
    {% if node is defined and node.field_hero_banner is not empty %}
      <div class="carousel-header">
        <div id="carouselId" class="carousel slide" data-bs-ride="carousel">

          <!-- Indicators -->
          <ol class="carousel-indicators">
            {% for item in node.field_hero_banner %}
              <li data-bs-target="#carouselId"
                  data-bs-slide-to="{{ loop.index0 }}"
                  class="{% if loop.first %}active{% endif %}">
              </li>
            {% endfor %}
          </ol>

          <!-- Slides -->
          <div class="carousel-inner" role="listbox">

            {% for item in node.field_hero_banner %}
              {% set slide = item.entity %}

              <div class="carousel-item {% if loop.first %}active{% endif %}">

                {% if slide.field_slide_image.entity %}
                  <img src="{{ file_url(slide.field_slide_image.entity.fileuri) }}"
                       class="d-block w-100 img-fluid"
                       alt="{{ slide.field_title.value }}">
                {% endif %}

                <div class="carousel-caption">
                  <h1>{{ slide.field_title.value }}</h1>
                  <p>{{ slide.field_slide_description.value }}</p>
                </div>

              </div>

            {% endfor %}

          </div>

          <!-- Controls -->
          <button class="carousel-control-prev" type="button" data-bs-target="#carouselId" data-bs-slide="prev">
            <span class="carousel-control-prev-icon bg-secondary" aria-hidden="true"></span>
            <span class="visually-hidden">Previous</span>
          </button>

          <button class="carousel-control-next" type="button" data-bs-target="#carouselId" data-bs-slide="next">
            <span class="carousel-control-next-icon bg-secondary" aria-hidden="true"></span>
            <span class="visually-hidden">Next</span>
          </button>

        </div>
      </div>
    {% endif %}
    <!-- Homepage Carousel End -->

    <!-- Services Start -->
        {# {{ page.homepage_services }} #}
    <!-- Services End -->

     <!-- training Start -->
      <div id="skip_main_content"> {{ page.homepage_training }} </div>
    <!-- training End -->

    <!-- chairship start -->
    <section class="chairship-section">
      <div class="container-fluid px-5">
        <!-- Top Heading -->
        <div class="row">
          <div class="col-12">
            <h1 class="main-title"> {{ chairship.label() }} </h1>
            <div class="top-description">
              {{ chairship.body.processed|raw }}
            </div>
          </div>
        </div>
        <!-- Content -->
        <div class="row align-items-start">
          <!-- Left Content -->
          <div class="col-lg-5">
            <div class="content-box">
              <h3>Key Activities</h3>
              <ul>
                {% for item in chairship.field_key_activities %}
                {% set paragraph = item.entity %}
                <li> {{ paragraph.field_activities_title.value }} </li>
                {% endfor %}
              </ul>

              <h3>Thematic Pillars and Priorities</h3>
              <ul>
                 {% for item in chairship.field_thematic_pillars_and_prior %}
                  {% set paragraph = item.entity %}
                    <li> {{ paragraph.field_pillars_priorities_title.value }} </li>
                 {% endfor %}
              </ul>
            </div>
          </div>
          <!-- Right Images -->
          <div class="col-lg-7">
            <div class="row image-grid g-0">
              {% if chairship.field_chairship_image.entity %}
                {% set bg = file_url(chairship.field_chairship_image.entity.fileuri) %}
              {% endif %}

              <div class="col-md-6">
                <div class="left-image">
                  <img src="{{ bg }}" alt="">
                </div>
              </div>

              <div class="col-md-6">
                <div class="right-images">
                  <img src="images/chairship-top.jpg" alt="">
                  <img src="images/chairship-bottom.jpg" alt="">
                </div>
              </div>

            </div>
          </div>

        </div>
      </div>
    </section>



   <!-- Statistic Section start by custom block content (Home statistical data)-->

  <section class="about-section">
    <div class="container-fluid px-5">
    <h2 class="about-title">
      About IIIDEM (India International Institute of Democracy and Election Management)
    </h2>
    <div class="row">
      <!-- Left Content -->
      <div class="col-lg-4">
        <div class="about-left">
          <div class="logo-box">
             {% if home_about_block.field_image.entity %}
              {% set logo_img = file_url(home_about_block.field_image.entity.fileuri) %}
            {% endif %}
            <img src="{{logo_img}}" alt="IIIDEM Logo">
          </div>
          <p class="about-text">
            {{ home_about_block.field_description.value|striptags }}
          </p>
          <a href="/about-us" class="read-btn">
            Read More <i class="bi bi-arrow-up-right"></i>
          </a>
        </div>
      </div>

      <!-- Right Stats -->
      <div class="col-lg-8">
       <div class="stats-wrapper">
        <!-- Dynamic Background Image -->
          {% if home_about_block.field_home_iiidem_image.entity %}
            {% set iiidem_img = file_url(home_about_block.field_home_iiidem_image.entity.fileuri) %}
          {% endif %}
          <img src="{{ iiidem_img }}" alt="Background Image" class="stats-bg">

            <!-- Blue Overlay -->
            <div class="stats-overlay"></div>
            <!-- Cross Lines -->
            <div class="cross-horizontal"></div>
            <div class="cross-vertical"></div>
            <!-- Dynamic Statistics -->
            <div class="stats-grid">
              <!-- Items -->
              {% for item in home_about_block.field_statistics %}
                {% set stat = item.entity %}
              <div class="stat-box">
                <div class="stat-icon">
                   {% if stat.field_icon.entity %}
                  <img src="{{ file_url(stat.field_icon.entity.uri.value) }}" alt="">
                  {% endif %}
                </div>
                <h2 class="stat-number">{{ stat.field_number.value }}+</h2>
                <p class="stat-text">
                  {{ stat.field_label.value }}
                </p>
              </div>
              {% endfor %}
            </div>
          </div>
          </div>
        </div>
      </div>
    </section>


      <!-- Statistic Section end -->

       <!-- Event Start -->
      <section class="py-5">
         {{ page.homepage_event }}
      </section>
      <!-- Event End -->

      <section class="emb-section">
  <div class="container-fluid px-5">

    <div class="row g-3 align-items-stretch">

      <!-- Left Section -->
      <div class="col-lg-5">
        <div class="left-panel">

          <div class="left-content">

            <div class="left-text">
              <h2>
                Collaboration & Partnerships with Other Election Management Bodies (EMBs)
              </h2>
            </div>

            <div class="left-image">
              <img src="https://images.unsplash.com/photo-1522202176988-66273c2fd55f?q=80&w=1200&auto=format&fit=crop" alt="">
            </div>

          </div>

        </div>
      </div>

      <!-- Card 1 -->
      <div class="col-lg col-md-4 col-6">
        <div class="emb-card">
          <div>
            <div class="emb-logo">
              <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/5/59/Flag_of_Bosnia_and_Herzegovina.svg/1200px-Flag_of_Bosnia_and_Herzegovina.svg.png" alt="">
            </div>

            <div class="emb-title">
              Central Election Commission of BiH
            </div>
          </div>

          <div class="emb-bottom"></div>
        </div>
      </div>

      <!-- Card 2 -->
      <div class="col-lg col-md-4 col-6">
        <div class="emb-card">
          <div>
            <div class="emb-logo">
              <img src="https://upload.wikimedia.org/wikipedia/commons/9/91/Flag_of_Bhutan.svg" alt="">
            </div>

            <div class="emb-title">
              Election Commission of Bhutan
            </div>
          </div>

          <div class="emb-bottom blue-1"></div>
        </div>
      </div>

      <!-- Card 3 -->
      <div class="col-lg col-md-4 col-6">
        <div class="emb-card">
          <div>
            <div class="emb-logo">
              <img src="https://upload.wikimedia.org/wikipedia/commons/f/f9/Flag_of_Bangladesh.svg" alt="">
            </div>

            <div class="emb-title">
              Bangladesh Election Commission
            </div>
          </div>

          <div class="emb-bottom blue-2"></div>
        </div>
      </div>

      <!-- Card 4 -->
      <div class="col-lg col-md-4 col-6">
        <div class="emb-card">
          <div>
            <div class="emb-logo">
              <img src="https://upload.wikimedia.org/wikipedia/commons/9/9a/Flag_of_Afghanistan.svg" alt="">
            </div>

            <div class="emb-title">
              Independent Election Commission (Afghanistan)
            </div>
          </div>

          <div class="emb-bottom blue-3"></div>
        </div>
      </div>

      <!-- Card 5 -->
      <div class="col-lg col-md-4 col-6">
        <div class="emb-card">
          <div>
            <div class="emb-logo">
              <img src="https://upload.wikimedia.org/wikipedia/commons/9/9b/Flag_of_Nepal.svg" alt="">
            </div>

            <div class="emb-title">
              Election Commission of Nepal
            </div>
          </div>

          <div class="emb-bottom"></div>
        </div>
      </div>

    </div>

  </div>
</section>

  <!-- ===============================
    IIIDEM international_organizations Setion
    ================================ -->
    <section class="emb-section">
      <div class="container-fluid">
        <!-- Heading -->
        <div class="row">
          <div class="col-lg-7">
            <h2 class="emb-title">
              {{ international_organizations.label() }}
            </h2>
          </div>
        </div>
        <!-- Timeline -->
        <div class="timeline-wrapper">
          <div class="timeline-items">
            <!-- Item -->
             {% for item in international_organizations.field_partnership_logo %}
              {% if item.entity and item.entity.field_organization_logo.entity %}
            <div class="timeline-item">
              <div class="logo-box">
                <img src="{{ file_url(item.entity.field_organization_logo.entity.fileuri) }}"
                    alt="{{ item.entity.field_organization_logo.alt|default('Logo') }}" />
              </div>
            </div>
            {% endif %}
            {% endfor %}

          </div>
          <!-- Timeline Black Line -->
          <div class="timeline-line"></div>
        </div>

      </div>
    </section>

   <!-- ===============================
    IIIDEM mous Setion
    ================================ -->

       <section class="mou-section">
        <div class="container">
          <h2 class="mou-title">
            {{ mou_block_title  }}
          </h2>
          <div class="row g-4">
            <!-- Card -->
            <div class="row g-4">
              {% for logo in logos %}
                <div class="col-lg col-md-6">
                  <div class="mou-card">
                    <div class="mou-logo">
                      <img src="{{ logo.image }}" alt="">
                    </div>
                    <h3 class="mou-name">
                      {{ logo.description }}
                    </h3>
                  </div>
                </div>
              {% endfor %}


              <!-- MOU Slider -->
              <!-- <div class="swiper mouSwiper">
                <div class="swiper-wrapper">
                  {% for logo in logos %}
                    <div class="swiper-slide">
                      <div class="mou-card">
                        <div class="mou-logo">
                          <img src="{{ logo.image }}" alt="">
                        </div>
                        <h3 class="mou-name">
                          {{ logo.description }}
                        </h3>
                      </div>
                    </div>
                  {% endfor %}
                </div>
              </div>
 -->
              <!-- Slider -->
              <div class="partner-slider">
                <div class="partner-track">
                  {% for logo in logos %}
                    <div class="partner-item">
                       <img src="{{ logo.image }}" alt="">
                    </div>
                    <h3 class="mou-name">
                      {{ logo.description }}
                    </h3>
                  {% endfor %}
                </div>
              </div>


            </div>
          </div>
        </div>
      </section>

      <!-- About Section on Home page Start -->
      <div class="container">
        <div class="text-center mb-5">
          <span class="gov-line"></span>
          <h2 class="gov-title"> About IIIDEM</h2>
          <p><img src="https://iiidem.eci.gov.in/newimg/iiidem-vLogo.png" alt=""></p>
          <span class="gov-line"></span>
        </div>

        {% if about_tabs_node %}
          <div class="row g-4">
            {% for item in about_tabs_node.field_about_tabs %}
              {% set tab = item.entity %}
              <div class="col-lg-4 col-md-6">
                <div class="gov-card h-100 text-center">
                  <div class="gov-icon">
                    <i class="fas fa-file-signature"></i>
                  </div>
                  <h3>{{ tab.field_tab_title.value }}</h3>
                  <p>
                    {{ tab.field_about_tab_content.value|striptags|slice(0,120) }}...
                  </p>
                 <a href="/about-us#tab{{ loop.index }}" class="gov-btn">
                  Explore Programs
                </a>
                </div>
              </div>
            {% endfor %}
          </div>
        {% endif %}
    </div>
  <!-- About Section End -->


  <!-- =========================================
     Media Gallery Section (Government Style)
     Image Gallery + Video Gallery
========================================= -->

<section class="media-section py-5">
  <div class="container">

    <!-- Section Heading -->
    <div class="section-heading text-center mb-5">
      <span class="gov-line"></span>
      <h2 class="gov-title">Media Gallery</h2>
      <span class="gov-line"></span>
    </div>

    <div class="row g-4">

      <!-- ===============================
           Image Gallery
      =============================== -->
      <div class="col-lg-6">

        <div class="gov-media-box">
          <div class="media-title">
            <i class="fas fa-image"></i> IIIDEM Image Gallery
          </div>
         <!--  <span class="gov-line"></span> -->

          {{ page.homepage_image_gallery }}

          <div class="text-center mt-4">
            <a href="/photo-gallery" class="gov-btn">View All Photos</a>
          </div>
        </div>

      </div>


      <!-- ===============================
           Video Gallery
      =============================== -->
      <div class="col-lg-6">
        <div class="gov-media-box">
          <div class="media-title">
            <i class="fas fa-video"></i> Video Gallery
          </div>
          <div class="ratio ratio-16x9 mb-3">
            <iframe width="360" height="315" src="https://www.youtube.com/embed/J-51AZHKcCo?si=peB6cxWgZuwzjzxz" ></iframe>
          </div>
          <div class="text-center mt-4">
            <a href="/video-gallery" class="gov-btn">View All Videos</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ===============================
IIIDEM Global Training Footprint Animated Bar Chart Version
================================ -->

<section class="footprint-section py-5">
  <div class="container">
    <!-- Heading -->
    <div class="section-heading text-center mb-5">
      <span class="gov-line"></span>
      <h2 class="gov-title">{{ global_footprint.label() }}</h2>
      {% if global_footprint %}
        <p>
          IIIDEM Presence Across
          <span class="highlight">
            {{ global_footprint.field_total_region_countries.value }}
          </span>
          Countries
        </p>
      {% endif %}
      <span class="gov-line"></span>
    </div>

    <!-- Chart -->
     {# Find MAX value #}
      {% set max = 0 %}
      {% for item in global_footprint.field_total_countries %}
        {% set val = item.entity.field_region_value.value|number_format %}
        {% if val > max %}
          {% set max = val %}
        {% endif %}
      {% endfor %}

    <div class="row">
      <div class="col-sm-6">
        <div class="chart-wrapper">
          {% for item in global_footprint.field_total_countries %}
            {% set region = item.entity %}
            {% set value = region.field_region_value.value %}
            {% set percent = (value / max) * 100 %}
            {% set color = region.field_color.value|default('#3b82f6') %}
            <div class="chart-row">
              <div class="chart-label">
                {{ region.field_region_name.value }}
              </div>
              <div class="chart-bar">
                <div class="chart-fill"
                     data-width="{{ percent }}"
                     style="background-color: {{ color }};">
                </div>
              </div>
              <div class="chart-value" data-value="{{ value }}">  0 </div>
            </div>
          {% endfor %}
        </div>
      </div>
   
      <div class="col-sm-6">
        {% if global_footprint.field_footprint_image.entity %}
          <img  src="{{ file_url(global_footprint.field_footprint_image.entity.fileuri) }}"
            alt="{{ global_footprint.field_footprint_image.alt }}" class="img-fluid rounded" >
        {% endif %}
      </div>
    </div>
  </div>
</section>

<!-- ===============================
IIIDEM partners Setion
================================ -->

<!-- {% if partners.field_partner_section_background.entity %}
  {% set bg = file_url(partners.field_partner_section_background.entity.fileuri) %}
{% endif %}

<section class="media-section py-5" style="background-image: url('{{ bg }}');">

    <div class="section-heading text-center mb-5">
      <span class="gov-line"></span>
      <h2 class="gov-title">{{ partners.label() }}</h2>
      <span class="gov-line"></span>
    </div>
    <div class="partner-slider">
      <div class="partner-track">
        {% for item in partners.field_partners_slider %}
          {% set paragraph = item.entity %}
          {% if paragraph.field_partner_logo_image.entity %}
            <div class="partner-item">
              <img  src="{{ file_url(paragraph.field_partner_logo_image.entity.fileuri) }}"
                alt="{{ paragraph.field_partner_logo_image.alt }}" >
            </div>
          {% endif %}
        {% endfor %}
      </div>
    </div>
  </div>
</section> -->


    <section class="social-section">

  <div class="container">

    <div class="social-wrapper">

      <div class="follow-text">
        Follow us
      </div>

      <div class="social-links">

        <!-- Facebook -->
        <a href="#" class="social-item">
          <span class="icon-circle facebook">
            <i class="fa-brands fa-facebook-f"></i>
          </span>
         <div> Facebook</div>

        </a>

        <!-- Instagram -->
        <a href="#" class="social-item">
          <span class="icon-circle instagram">
            <i class="fa-brands fa-instagram"></i>
          </span>
          Instagram
        </a>

        <!-- Twitter -->
        <a href="#" class="social-item">
          <span class="icon-circle twitter">
            <i class="fa-brands fa-x-twitter"></i>
          </span>
          Twitter
        </a>

        <!-- Youtube -->
        <a href="#" class="social-item">
          <span class="icon-circle youtube">
            <i class="fa-brands fa-youtube"></i>
          </span>
          Youtube
        </a>

      </div>

    </div>

  </div>

</section>


</main>

{% include '@iiidem_theme/components/footer/footer.twig' %}